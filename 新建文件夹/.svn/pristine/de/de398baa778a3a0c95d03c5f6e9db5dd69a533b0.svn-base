import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { RingTableComponent } from './ring-table.component';

describe('RingTableComponent', () => {
  let component: RingTableComponent;
  let fixture: ComponentFixture<RingTableComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ RingTableComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(RingTableComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
